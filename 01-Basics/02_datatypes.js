"use strict" //this means treat all js code as newer version

//alert(3+3) //runs in browser but not here

console.log(3 + 3)

let name = "pavan" //string
let age = 20 //integer
let flag = false //boolean

//integer range is 2 to the power 53
//for bigger integer than this we can use bigint
//null - standalone value
//undefined - value which is not defined
//symbol - for unique elements

//objects -- will see this later in detail

console.log(typeof "pavanmaske") 
console.log(typeof age);

console.log(typeof null); //output is object...which is drawback of lang
console.log(typeof undefined); //output is undefined only
